# Vehicle Fault Diagnostics Client

React + TypeScript + Vite client for demonstrating the Drools vehicle diagnostics system.

```powershell
npm install
npm run dev
```

The Vite dev server runs on `http://localhost:5173` and proxies `/api` to the Spring Boot service at `http://localhost:8080`.
